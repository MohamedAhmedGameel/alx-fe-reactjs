export const useAuth = () => {
    const isAuthenticated = true; // Replace with real authentication logic
    return { isAuthenticated };  // Example: fetch token or user data from context or state
};